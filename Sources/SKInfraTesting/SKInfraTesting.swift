// SKInfraTesting — public mocks and test doubles for every SKCore protocol.
//
// This module is intentionally consumed only from test targets. Its public
// types intentionally trade a bit of API surface area for ergonomic
// behavior verification (call records, virtual-time clocks, in-memory
// stores). They are **not** intended for production use.
