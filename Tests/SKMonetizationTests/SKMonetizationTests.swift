import Testing

@Suite("SKMonetization")
struct SKMonetizationTests {
    // RevenueCatAdapter requires a configured Purchases instance
    // and cannot be meaningfully unit-tested without it. This target
    // is reserved for future integration tests.
}
